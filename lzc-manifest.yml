application:
  subdomain: zen
  background_task: true
  public_path:
    - /
  upstreams:
    - location: /
      backend: http://zen:8080/
services:
  zen:
    # upstream: ghcr.io/sheshbabu/zen/zen:1.5.0
    image: registry.lazycat.cloud/czyt/sheshbabu/zen/zen:25b0075678f125f2
    environment:
      - DATA_FOLDER=/data
      - IMAGES_FOLDER=/images
      - PORT=8080
    binds:
      - /lzcapp/var/data:/data
      - /lzcapp/var/images:/images
